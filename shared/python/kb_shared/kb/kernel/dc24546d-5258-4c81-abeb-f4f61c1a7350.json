{
  "adr_id": "dc24546d-5258-4c81-abeb-f4f61c1a7350",
  "title": "Fix BuilderAgent path generation in backend/agents/builder.py:\n\nProblem: Builder...",
  "decision": "Fix BuilderAgent path generation in backend/agents/builder.py:\n\nProblem: BuilderAgent generates placeholder paths like 'path/to/function.py' instead of real paths.\n\nSolution in _analyze method:\n1. Extract module/function name from problem.context\n2. If problem.metadata has 'target_file', use it\n3. Otherwise, infer path from context:\n   - For new files: use backend/ or tests/ based on content\n   - For functions: ask for file path or use sensible default\n4. Replace placeholder logic with:\n   ```python\n   # Infer target path\n   target_path = problem.metadata.get('target_file')\n   if not target_path:\n       # Extract from context or use convention\n       if 'test' in problem.title.lower():\n           target_path = 'tests/test_generated.py'\n       else:\n           target_path = 'backend/generated_code.py'\n   ```Update OrgSidebar component in web/src/ui/OrgSidebar.tsx to dynamically load agents from backend API:\n\nChanges needed:\n1. Add API fetch in component:\n   ```typescript\n   const [agents, setAgents] = useState([]);\n   \n   useEffect(() => {\n     fetch('http://localhost:8000/agents')\n       .then(res => res.json())\n       .then(data => setAgents(data))\n       .catch(err => console.error('Failed to load agents:', err));\n   }, []);\n   ```\n\n2. Remove hardcoded agent list\n\n3. Map agents dynamically:\n   ```typescript\n   {agents.map(agent => (\n     <Link key={agent.id} to={`/agent/${agent.id}`}>\n       {agent.name}\n     </Link>\n   ))}\n   ```\n\n4. Add VerificationAgent and TestingAgent to display\n\nThis makes UI aware of new agents automatically.\n\nThis fixes the 'path/to/' issue permanently.",
  "context_summary": "Fix BuilderAgent path generation in backend/agents/builder.py:\n\nProblem: BuilderAgent generates placeholder paths like 'path/to/function.py' instead of real paths.\n\nSolution in _analyze method:\n1. Extract module/function name from problem.context\n2. If problem.metadata has 'target_file', use it\n3. Otherwise, infer path from context:\n   - For new files: use backend/ or tests/ based on content\n   - For functions: ask for file path or use sensible default\n4. Replace placeholder logic with:\n   ```py",
  "full_context": "Fix BuilderAgent path generation in backend/agents/builder.py:\n\nProblem: BuilderAgent generates placeholder paths like 'path/to/function.py' instead of real paths.\n\nSolution in _analyze method:\n1. Extract module/function name from problem.context\n2. If problem.metadata has 'target_file', use it\n3. Otherwise, infer path from context:\n   - For new files: use backend/ or tests/ based on content\n   - For functions: ask for file path or use sensible default\n4. Replace placeholder logic with:\n   ```python\n   # Infer target path\n   target_path = problem.metadata.get('target_file')\n   if not target_path:\n       # Extract from context or use convention\n       if 'test' in problem.title.lower():\n           target_path = 'tests/test_generated.py'\n       else:\n           target_path = 'backend/generated_code.py'\n   ```Update OrgSidebar component in web/src/ui/OrgSidebar.tsx to dynamically load agents from backend API:\n\nChanges needed:\n1. Add API fetch in component:\n   ```typescript\n   const [agents, setAgents] = useState([]);\n   \n   useEffect(() => {\n     fetch('http://localhost:8000/agents')\n       .then(res => res.json())\n       .then(data => setAgents(data))\n       .catch(err => console.error('Failed to load agents:', err));\n   }, []);\n   ```\n\n2. Remove hardcoded agent list\n\n3. Map agents dynamically:\n   ```typescript\n   {agents.map(agent => (\n     <Link key={agent.id} to={`/agent/${agent.id}`}>\n       {agent.name}\n     </Link>\n   ))}\n   ```\n\n4. Add VerificationAgent and TestingAgent to display\n\nThis makes UI aware of new agents automatically.\n\nThis fixes the 'path/to/' issue permanently.",
  "status": "DRAFT",
  "timestamp": "2026-01-22T08:00:03.639047+00:00",
  "logical_impact": "LEGACY_MIGRATION",
  "governance_phase": 11,
  "migrated": true,
  "consequences": [],
  "policies": []
}