# Knowledge Base Library (placeholder)

Planned contents:
- Storage + schemas currently in `backend/kb` and `shared/python/kb`.
- To be packaged as a reusable library (`pyproject.toml`) with clear API surface.
- Will carry ADR migration tools, storage adapters (SQLite), and schema definitions.
